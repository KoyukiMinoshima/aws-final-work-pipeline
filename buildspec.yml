version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name XXX --s3-location s3://final-work-bucket-2026-mk/artifact.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src